# NameCraft admin panel env — copy to client-admin/.env
# Only PUBLIC values belong here.

# Base URL of the shared backend API (same backend as the customer app).
VITE_API_BASE_URL=http://localhost:5000/api
