# NameCraft customer storefront env — copy to client-customer/.env
# Only PUBLIC values belong here (this is shipped to the browser).

# Base URL of the shared backend API.
VITE_API_BASE_URL=http://localhost:5000/api

# Public keys used later (Phase 3/4). Safe to expose.
VITE_RAZORPAY_KEY_ID=
VITE_CLOUDINARY_CLOUD_NAME=
